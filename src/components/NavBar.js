import React from 'react';

const NavBar = () => {
    return (
        <header>
            <h1>Find:</h1>
            <button className="HeaderButton">Restart</button>
        </header>
    )
}

export default NavBar;